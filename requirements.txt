# VERTEX — dépendances. Installées automatiquement par les scripts Lancer_VERTEX.
# Bornes sûres : versions récentes testées, sans casser sur une future version majeure.
flask>=3.0
yfinance>=0.2.40
pandas>=2.0,<4
numpy>=1.24,<3
ib_async>=1.0            # connexion IBKR LECTURE SEULE (live si TWS/Gateway ouvert)
anthropic>=0.30          # briefs IA en français (optionnel — clé ANTHROPIC_API_KEY)
python-dotenv>=1.0       # charge le fichier .env (code d'accès, clés)
gunicorn>=21.0           # serveur de production (cloud)
# Importés DIRECTEMENT par le produit, et jusqu'ici seulement présents comme
# dépendances transitives (requests via yfinance, markupsafe via flask). Ils
# fonctionnaient — et cassaient le jour où l'intermédiaire changerait de crémerie.
# markupsafe.escape sert à l'ÉCHAPPEMENT HTML de /api/analysis : adosser une
# primitive de sûreté à une dépendance qu'on ne déclare pas est le cas le moins
# confortable des deux. Mesuré par tools/mesures/mesurer_exploitation.py.
requests>=2.31,<3        # appels HTTP directs (briefs, news_plus, constituents)
markupsafe>=2.1,<4       # escape() — sûreté XSS des sorties analysis_api
