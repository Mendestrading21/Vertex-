# Secrets — NE JAMAIS commiter
.env

# État local (diff jour/jour du brief)
daily_prev.json

# Snapshot hebdo figé (watchlist de la semaine)
weekly_snapshot.json
weekly_snapshot.json.tmp

# Données personnelles du desk (positions réelles, sync appareils) — JAMAIS sur GitHub
desk_data.json
# Suivis analytiques hypothétiques (runtime) — jamais sur GitHub
tracking.json
tracking_backup_*.json

# Caches locaux régénérés automatiquement
*_cache.json
breadth_history.json
paper_state.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Captures d'écran de design (régénérables, mode démo) — pas dans le repo
docs/screenshots/
.vertex_secret
# Donnees runtime locales (regenerees par l'app — jamais dans le repo)
edge_ledger.jsonl
track_meta.json
alerts_fired.json
desk_backup_*.json
