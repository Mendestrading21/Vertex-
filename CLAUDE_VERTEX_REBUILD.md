# Historique — Vertex Total Rebuild

La phase Total Rebuild et le workflow Skyler V2 sont archivés. Ils ont produit
des audits, moteurs, tests et références utiles, mais ils ne pilotent plus le
développement.

L'unique entrée active est:

```text
/vertex-1-0
```

Lire `.claude/skills/vertex-1-0/SKILL.md` puis `docs/vertex-1.0/README.md`.
Ne jamais recréer une branche d'intégration parallèle à partir de ce document.
