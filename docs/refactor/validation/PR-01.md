# Validation — PR n°1 : Fondations honnêtes & responsive

> Branche `agent/vertex-total-rebuild`. Périmètre transverse (pas de refonte de
> page). READONLY intact, aucun chemin d'ordre, aucun moteur modifié pour une
> raison esthétique.

## Commits testés

1. `docs(refactor)` — audit phases 0-2 (8 documents).
2. `fix(v1-palette)` — palette source unique + test durci (C-02/C-11/C-03).
3. `chore(v1-hygiene)` — untrack `company_cache.json` + `position_inventory.json` ;
   identité Obsidian Copper/Inter (C-04).
4. `test(v1-decision)` — garde-fou décision-sans-données ; requalif C-08.
5. `fix(v1-data)` — `/api/data-quality` étiquette la démo `DEMO` (C-07).
6. `fix(v1-responsive)` — débordement horizontal mobile + bump SW v43.

## Environnement & commandes

- Conteneur Linux, Python 3.11.15, `DEMO=1 NO_IBKR=1 PORT=5002`.
- `python -m compileall -q terminal.py vertex` → **exit 0**.
- `python -m pytest tests/ -q` → **899 passed, 2 skipped** (baseline : 893 → +6
  tests ajoutés, aucun désactivé).
- Serveur démo relancé à chaque changement de runtime ; `/healthz` → 200.

## Résultats par correctif

| Réf. | Correctif | Preuve |
|---|---|---|
| C-02/C-11 | Palette `palette.py` canonique, thème JS + repli `chart-core` alignés | `test_js_theme_matches_python_palette` compare la série entière ; `test_chart_core_fallback_series_matches_palette` ajouté ; 32 tests visuels verts |
| C-03 | Commentaires « cuivre/orange » purgés | thème JS décrit « Vert Signal » |
| C-04 | Identité = Obsidian Copper / Inter | aucune trace « Obsidian Prism/General Sans » hors docs d'audit ; `tokens.css` Inter + IBM Plex Mono |
| Hygiène | `company_cache.json` (1,38 Mo) + `position_inventory.json` untrackés | `git ls-files` ne les liste plus ; fichiers conservés localement ; `.gitignore` mis à jour |
| C-07 | `/api/data-quality` en démo étiquette `DEMO` (pas `MISSING`) | live : `by_quality {DEMO:20}, degraded 0` ; `test_data_quality_demo.py` (4 tests) |
| C-08 | Décision sans données déjà honnête (faux positif requalifié) | `test_unknown_symbol_is_honestly_insufficient` : `DATA_INSUFFICIENT`, `confidence 0` |
| Responsive | 0 débordement contenu sur 11 pages en 390px | voir tableau ci-dessous |

## Débordement horizontal mobile (390×844) — avant / après

`realMax` = bord droit max d'un élément **hors conteneur de scroll intentionnel**
(overflow-x:auto/clip/fixed exclus). C'est la mesure honnête du contenu coupé.

| Page | Avant (scrollWidth) | Après realMax | Verdict |
|---|---|---|---|
| Briefing | 430 | **390** | OK |
| Marchés | 469 | **390** | OK (scrollWidth résiduel 463 = tables/heatmaps en scroll) |
| Opportunités | 456 | **390** | OK |
| Portefeuille | 403 | **390** | OK (résiduel 403 = scroll intentionnel) |
| Analyse | 390 | 390 | OK |
| Options | 390 | 390 | OK |
| Performance | 417 | **390** | OK (résiduel 417 = scroll intentionnel) |
| Intelligence | 405 | **390** | OK |
| Système | **547** | **390** | OK (pire cas corrigé) |
| Suivi | 448 | **390** | OK (résiduel 448 = scroll intentionnel) |
| Design system | 390 | 390 | OK |

**Desktop 1440 : aucun débordement avant comme après.**

## Contrôles transverses

- `/api/client-log` → `{"count":0,"errors":[]}` (0 erreur console réelle).
- `/sw.js` sert `td-shell-v43` ; gardiens SW mis à jour (3 tests).
- États honnêtes préservés : IBKR OFFLINE / TV MISSING / IA MISSING étiquetés ;
  badge démo visible ; ticker absent → nuls.
- READONLY : `READONLY=True` inchangé ; `test_no_orders` vert ; aucun moteur
  financier modifié (seul un test de non-régression ajouté).

## Limites honnêtes

- Inspection via Chromium **headless** (pas d'appareil physique) — ergonomie
  tactile réelle iPhone/iPad à confirmer côté utilisateur.
- Résidu de `scrollWidth` sur 4 pages = contenu en scroll horizontal intentionnel
  (tables/heatmaps), conforme à la règle design §9 ; à revoir page par page lors
  des PR de refonte pour convertir certaines tables en cartes si souhaité.
- Fallbacks inline `#8f8a83` subsistent dans quelques pages (résolus au runtime
  vers la valeur canonique via `VXCharts.colors`) — nettoyage cosmétique différé.

## Verdict

**GO.** Tous les défauts transverses critiques de l'audit sont traités ou
requalifiés avec preuves ; tests 100 % ; responsive mobile assaini sans toucher
au desktop ni aux moteurs. Prêt pour la revue, puis PR n°2 (design system &
chart system).
