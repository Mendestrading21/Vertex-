# Décisions actives — Vertex 1.0

| ID | Décision | Statut |
|---|---|---|
| D-001 | `main` est l'unique base de consolidation; aucune ancienne intégration n'est fusionnée en bloc. | active |
| D-002 | Nom produit unique: Vertex. « Skyler » reste un nom technique historique, pas une architecture parallèle. | active |
| D-003 | Entrée locale `python -m vertex`; WSGI `vertex.runtime:app`; `terminal.py` devient adaptateur. | active |
| D-004 | Options: 2/4/6 semaines, DTE préféré 120–240, cible 180. | active |
| D-005 | Actions: horizons 3/6/12 mois. | active |
| D-006 | WMB est contexte macro sourcé, jamais source de prix ou hard-gate override. | active |
| D-007 | TradingView demande une réévaluation, jamais une exécution. | active |
| D-008 | Un seul skill Claude: `/vertex-1-0`. | active |
| D-009 | Signal OS sera extrait composant par composant après comparaison, jamais fusionné globalement. | active |
| D-010 | Vertex 1.0 reste RC jusqu'à validation de tous les release gates sur le même SHA. | active |
| D-011 | Le runtime canonique active le corpus V4; le lancement direct de `terminal.py` conserve V3 uniquement comme rollback transitoire. | active |
| D-012 | Une capacité n'est « terminée » que si elle satisfait `QUALITY_STANDARD.md`. | active |
| D-013 | Les suppressions legacy suivent `CLEANUP_POLICY.md`; aucune suppression de masse aveugle. | active |
| D-014 | Sync du desk : **une clé omise par un push est conservée**, car aucun chemin du produit ne supprime une clé de localStorage — une absence est toujours un défaut de lecture. Supprimer se dit en envoyant la clé **vide**. Un instantané `desk_avantperte_*` est pris à la seconde dès qu'une clé est menacée. | active |
| D-015 | `performance_ledger` est **conservé**, pas supprimé. La preuve de non-usage existe (aucun chemin de production ne l'atteint), mais `CLEANUP_POLICY.md` exige **cinq** conditions et deux échouent : il n'a **aucun remplacement canonique en production** — rien d'autre n'implémente la discipline SIGNAL → ALERT → RECOMMENDATION → USER_DECISION → SIMULATED → REAL — et **trois fichiers de tests l'importent**. La vraie question n'est donc pas « le supprimer ? » mais « le brancher ? », et c'est une décision produit. | active |
| D-016 | Les 63 règles CSS **prouvées** inatteignables sont **recensées et gelées**, pas supprimées. Le plafond dans `tests/test_vertex_1_0_regles_mortes.py` **est** la « date de retrait » que `CLEANUP_POLICY.md` exige : il peut baisser, il ne peut pas monter en silence. La preuve a eu **trois trous** avant d'être fiable (corpus limité aux 8 espaces → 29 fausses preuves ; routes paramétrées absentes ; noms de classe assemblés à l'exécution) — ce qui justifie de garder la preuve reproductible plutôt que d'agir une fois. | active |
| D-017 | La preuve doit être reproductible **sur la machine où l'on décide**. Les gardiens ne comparent plus des chemins ni des octets dépendants de l'OS (séparateurs, CRLF, décodage cp1252 des sorties Node), et la disponibilité du navigateur se mesure par un **lancement réel**, jamais par la présence d'un fichier. Motif : sur Windows — la machine qui a TWS, donc celle où G5 se valide — la suite imposée par `CLAUDE.md` était rouge (25 échecs) et les gates navigateur G4 s'abstenaient partout, `_chromium()` ne connaissant que `/opt/pw-browsers/`. Une preuve qu'on ne peut pas rejouer là où l'on décide n'est pas une preuve. | active |
| D-018 | Un gardien de sécurité qui crie sur l'actualité ordinaire est désarmé. Le motif de clé d'API n'admet les tirets que derrière un préfixe d'émetteur (`sk-ant-`, `sk-proj-`) : une dépêche citant **SK Hynix** faisait virer le contrôle de fuite au rouge sur `/news-feed` à chaque passage. L'URL fautive est conservée dans le corps témoin SAIN, pour que le resserrement soit tenu. | active |
| D-019 | **Le courtier d'abord, le web ensuite.** IBKR est la tête de la chaîne de données du scan ; yfinance puis stooq ne ramassent que ce qu'il ne sert pas. Mesuré sur U<masque> : 515/533 symboles par IBKR (97 %), 18 au repli — et 141/141 sur la série longue. Motif : faire cohabiter deux origines pour un même titre, c'est accepter que le scan et l'écran ne parlent pas du même cours, alors que cotations, chaînes d'options et P&L viennent déjà du compte. Coût assumé : le scan passe de ~30 s à ~190 s (une requête par titre contre des lots de 50). La provenance NOMME chaque contributeur (`ibkr+yfinance` ≠ `ibkr`) et compte les titres par source : un repli invisible est un mensonge de source. | active |
| D-020 | Restent au repli **par entitlement mesuré**, pas par choix : les fondamentaux (IBKR **10358** « Fundamentals data is not allowed »), l'indice NDX (**354** « not subscribed ») et les symboles sans définition de titre chez IBKR (mesuré : AVB). Ces trois cas doivent produire une ABSENCE côté courtier — jamais un trou rempli — pour que le repli les ramasse et que l'écran puisse le dire. | active |
| D-021 | Le rôle IBKR `historique` a son propre identifiant client (23) et **une seule session à la fois** (verrou). Deux boucles appellent le module — scan de l'univers et série longue — et partaient ensemble : TWS refusait la seconde, et l'échec remontait en « TWS injoignable », c'est-à-dire en accusation contre le courtier pour une collision née chez nous. | active |
| D-022 | **Un widget trop lent est un widget vide.** Le balayage des 92 surfaces comptait six routes « en erreur » alors qu'elles rendaient 200 avec leurs données : elles mettaient 19 à 27 s, et le navigateur abandonne avant. Le graphe de connaissance se reconstruisait entièrement sur les 515 titres à chaque requête, sans cache, et la route par symbole le refaisait avant de propager — il est mémoïsé, avec une clé qui nomme TOUT ce dont il dépend (scan, univers, calendrier, horodatage de `desk_data.json`, car les positions changent hors du scan). Mesuré : 26,7 s → 0,012 s. | active |
| D-023 | La borne de fraîcheur d'un cache doit être **plus longue que la latence qu'elle corrige**. Premier essai à 2 s sur les positions courtier : aucun effet (49,5 → 48,2 s), parce qu'un appel dure ~20 s et que la mémoire avait toujours expiré avant la route suivante — on corrigeait la redondance quand le coût est la latence. À 15 s : page Portefeuille 49,5 → 28,0 s, `/api/positions/alerts` 18,3 s → 6 ms. La borne ne périme pas la donnée davantage : elle a déjà une vingtaine de secondes quand elle s'affiche. | active |
| D-024 | **Limite NON corrigée, écrite pour ne pas être oubliée** : le premier aller-retour de `/api/positions/state` (18–31 s) et `/api/ibkr/positions` (9–11 s) reste lent. La cause est la file du worker options, partagée avec la rotation des chaînes — un défaut d'architecture antérieur qu'aucun cache ne résout. Un correctif qui tait ce qu'il ne corrige pas se lit comme un correctif complet. | active |
| D-025 | **Les dépêches viennent du courtier.** Le compte est abonné à des fournisseurs professionnels (Dow Jones, Briefing) ; le fil lisait yfinance puis un repli RSS. Mesuré : 12 symboles en 8,9 s sur UNE session, fil à 45 articles, provenance `dépêches ibkr` sans repli web. Piège tenu par un gardien : `reqHistoricalNews` rejette la requête ENTIÈRE (erreur 321) si un seul fournisseur de la liste n'est pas abonné — interroger les huit codes rendus par `reqNewsProviders` donnait zéro dépêche alors que quatre répondaient. On interroge fournisseur par fournisseur. IBKR n'expose pas d'URL publique : le lien reste vide, ce qui est l'aveu exact. | active |
| D-026 | **Une écriture DOM asynchrone se garde, ou aucune.** `/markets` a des vues : changer de vue remplace le DOM, et une requête en vol reprend la main sur un élément supprimé — `Cannot set properties of null (setting 'innerHTML')`, intermittent, longtemps mis sur le compte du hasard. Le fichier gardait DÉJÀ trois écritures et en laissait 21 nues : c'est l'incohérence qui laissait passer la course. Les 21 passent par `(...||{})`. Limite dite : le défaut n'a jamais pu être reproduit à volonté — la correction traite la cause lue dans le code, et le journal est propre depuis. | active |
| D-027 | **Une écriture DOM asynchrone se garde, partout ou nulle part.** D-026 n'avait traité que `/markets` — l'erreur est revenue le lendemain sur l'accueil, pendant une session live. Le recensement a montré **169 écritures nues dans 11 fichiers** (accueil 16, Système 42, Intelligence 33) : le défaut était produit-wide, pas propre à une page. Corriger le site où le symptôme apparaît laisse la classe entière en place. Plafond tenu à ZÉRO par `tests/test_vertex_1_0_dom_sur.py` : la forme gardée coûte une parenthèse, aucun cas ne justifie la forme nue. | active |
| D-028 | **Aucun nouveau score avant la fondation point-in-time.** Les fondamentaux actuels de `yfinance.info` ne peuvent pas prouver une performance historique sans risque de look-ahead. Toute nouvelle stratégie reste en recherche ou shadow tant que les observations, dates de disponibilité et révisions ne sont pas conservées. | active |
| D-029 | **Le profil V4 n'est pas modifié en place.** Les écarts entre sa catégorie `DYNAMIC` et la constitution utilisateur deviennent un profil V5 candidat, rétrotesté et soumis à décision humaine avant activation. | active |
| D-030 | **Sources officielles avant agrégateurs payants.** IBKR reste primaire pour marché/options/compte/news ; SEC, FRED, BLS et CFTC complètent la preuve. Massive, Benzinga, Finnhub ou OpenBB ne sont étudiés qu'après mesure d'un manque, revue de licence et budget. | active |
| D-031 | **Un gardien qui ne connaît qu'une orthographe du défaut certifie une page qu'il n'a pas lue.** Le gardien de D-027 ne cherchait que `$('x')` et rendait « 0 défaut » alors que **26** écritures DOM nues subsistaient : 16 par `getElementById` direct — dont une dans un `.catch()` de la fiche Analyse — et 10 sur `textContent`, forme que la transformation initiale ne touchait pas. L'erreur est revenue en session live sur `/portfolio` le lendemain. Le gardien couvre désormais trois formes de recherche × deux propriétés, avec DEUX témoins : l'un prouve qu'il voit chaque orthographe, l'autre qu'il ne signale PAS la forme gardée — un gardien qui refuse aussi la correction est désactivé au premier commit pressé. | active |
| D-032 | **`available_at`, jamais `observed_at`.** « Ce que Vertex savait à T » filtre sur la date de DISPONIBILITÉ. Un trimestre clos le 30 septembre et publié le 25 octobre n'existe pas pour une décision du 1er octobre. Filtrer sur la date d'observation — l'erreur naturelle — transformerait tout backtest en clairvoyance, ce que l'audit du 24 août nomme comme défaut P0. Le registre le rend **impossible**, il ne le déconseille pas. | active |
| D-033 | **Un ticker n'est pas une identité.** `FB` → `META` sans changer de société ; un ticker rendu peut être réattribué à une autre entreprise. Ordre d'autorité : conId IBKR → CIK SEC → ticker+place+devise, ce dernier marqué `fragile` pour que l'appelant puisse le refuser en connaissance de cause. La devise entre dans la clé : USD et EUR ne partagent pas une série de prix. | active |
| D-034 | **Un instant sans fuseau est refusé.** « 2026-10-25T20:05:00 » désigne vingt-six instants. L'interpréter en UTC par défaut est une supposition silencieuse qui décale les publications d'une demi-journée et inverse l'ordre de deux dépêches. Même refus pour l'instant de la QUESTION : demander « que savait-on à 20:05 » sans dire où est une question à vingt-six réponses. | active |
| D-035 | **Un split est une observation, pas une réécriture du passé.** Le facteur est enregistré à sa date de disponibilité ; l'ajustement se calcule à la lecture et reste explicable. Réécrire les prix stockés effacerait ce que Vertex a réellement vu — un registre qui réécrit son passé ne sert plus à expliquer une décision. | active |
| D-036 | **Une date de dépôt SEC devient connaissable à la FIN de sa journée, à New York.** La SEC ne publie que des dates ; le registre refuse un instant sans fuseau. Prendre le début de journée donnerait à un backtest jusqu'à vingt-quatre heures d'avance sur le marché — la faute même que la phase 2 rend impossible. Le fuseau est celui du déposant avec son heure d'été réelle : un décalage fixe se tromperait la moitié de l'année et déplacerait d'un jour les dépôts faits près de minuit. | active |
| D-037 | **Vertex n'invente aucun contact pour le fair-access SEC.** Le `User-Agent` est lu dans `SEC_USER_AGENT` et l'appel est REFUSÉ sans lui — avant même de construire l'URL, pour qu'aucune requête ne parte. Ni contact codé en dur, ni emprunt de l'adresse de l'utilisateur : une donnée personnelle ne part pas chez un tiers parce que le code en avait besoin. Un test vérifie l'absence de contact dans le module. | active |
| D-038 | **Un tag XBRL absent n'est jamais un zéro, une valeur non numérique n'est jamais coercée.** Un tag manquant est une ABSENCE, un zéro une AFFIRMATION — et une affirmation fausse se propage dans tous les ratios en aval. Les booléens sont écartés explicitement : `True` vaut 1 en Python et ferait entrer un drapeau XBRL dans une série numérique. Ce qui est écarté est COMPTÉ, jamais silencieux. | active |
| D-039 | **Une date indicative n'est pas un fait.** Le calendrier macro place le CPI « au 13 » par convention et déduit le NFP du premier vendredi ; seules les dates FOMC sont publiées. Le drapeau `approx` était **perdu** dans `events.build()` : le CPI du 13 septembre sortait `fact`/`DECLARED`, indiscernable de la décision FOMC du 16. `date_fiabilite` le porte désormais, une date de convention devient `estimate`, et le **doute est le défaut** — une source qui ne dit pas si sa date est publiée n'est pas crue sur parole. | active |
| D-040 | **`peut_fonder_un_gate()` : une garde posée avant la brèche.** Mesure faite avant le correctif : **aucun** des treize hard gates V4 ne consomme les événements — le pire cas décrit par l'audit n'était donc pas actif, et le dire évite de prétendre fermer une brèche ouverte. La garde vaut pour l'avenir : le jour où un gate de fenêtre d'événement s'écrira, il ne pourra pas accepter en silence une date que personne n'a publiée. Un test consigne l'absence actuelle et **tombera** si un futur lot en branche un sans passer par la garde. | active |
| D-041 | **Deux sources IBKR donnent deux P&L différents, et Vertex ne tranche pas.** Mesuré sur le compte réel le 24 août 2026 : `accountSummary` rend 1 024,03 USD de P&L non réalisé, `reqPnL` en rend 928,57 — **95,46 USD d'écart** au même instant. Ce n'est pas une anomalie de Vertex (les deux ne calculent pas sur la même base) ; ce qui en serait une, c'est d'en choisir une **en silence** : le P&L affiché deviendrait vrai ou faux selon un arbitrage que personne n'a pris. Les quatre sources sont lues, l'écart est nommé et chiffré, aucun gagnant n'est désigné. | active |
| D-042 | **La ligne `BASE` d'IBKR ne l'emporte jamais sur une devise réelle**, quel que soit l'ordre d'arrivée. `BASE` est un total converti ; le retenir mélangerait des montants convertis à des montants natifs. Un simple « premier arrivé » laisserait passer le défaut une fois sur deux — c'est pourquoi la règle est écrite sur la priorité, pas sur l'ordre. | active |
| D-043 | **Zéro source ne conclut pas à la concordance.** Une réconciliation sans aucune source rend `concordant: None`, jamais `True` : l'absence de mesure ne prouve rien, et le prétendre serait un mensonge confortable. La tolérance des arrondis est **plafonnée** — au-delà, la réconciliation ne réconcilie plus, elle décore, et le module refuse explicitement. | active |
| D-044 | **Le total ne dit pas où regarder : la réconciliation descend à la ligne.** L'écart global de 270,13 USD mesuré sur le compte réel venait d'**une seule** position — URA, valorisée 7 760,00 par Vertex contre 8 032,84 par le courtier — et de deux lignes suivies mais non détenues (AAPL, ADP). Trois familles nommées séparément : valorisation divergente, absente chez le courtier, absente chez Vertex. Elles n'appellent pas la même correction. | active |
| D-045 | **Le P&L par ligne vient de `portfolio()`, jamais de `positions()`.** `ib.positions()` rend le contrat et la quantité, pas la valorisation. Les confondre donnerait une comparaison ligne à ligne dont TOUS les P&L courtier seraient `None` — donc une réconciliation qui ne trouve jamais rien et **rassure à tort**. | active |
| D-046 | **D-023 refait par réflexe, et écrit là où il attend le prochain.** La borne du rapprochement P&L était à 15 s pour un appel qui dure **54,7 s** : la mémoire expirait toujours avant l'appel suivant et n'était jamais servie. Portée à 120 s — 30,4 s à froid, **0,002 s** ensuite. Un piège dans lequel on retombe mérite d'être nommé à l'endroit exact où il attend. | active |
| D-047 | **Une marque d'option dit d'où elle vient.** Mesuré sur URA 20270115 C 50 : marché 3,50/4,30, dernier échange 3,70 (du jour), milieu 3,90, clôture 3,88, marque IBKR 3,8546. Aucun de ces chiffres n'est faux — ce sont trois **conventions** qui divergent de 5 % parce que la fourchette est large de 20 %. L'écran affichait `mark 3,70` avec `last: None` et `mid: None` : l'origine était invisible, donc l'écart de 272 USD inexplicable. `mark_source`, `mid` et `last` sont désormais exposés. La convention n'est PAS changée — choisir entre les trois est un arbitrage (D-041), et un test garde l'absence de revalorisation. | active |
| D-048 | **Un marché large rend toute valorisation incertaine, et le produit le dit.** À 20,5 % de spread, dernier échange, milieu et marque du courtier s'écartent de plusieurs pour cent : afficher un P&L au centime promet une précision que la donnée n'a pas. `valorisation_incertaine` passe à `True` au-delà de 10 %. Sans fourchette connue elle vaut `None`, jamais `False` — rendre `False` ferait passer une ignorance pour une garantie. | active |
| D-049 | **Une preuve live qui ne se rejoue pas ne protège rien.** La session TWS du SHA `d77b06d` prouvait la connexion, mais n'exécutait aucune ligne des quatre adaptateurs IBKR — mesurés à 0 % par l'audit. `ibkr_replay.py` anonymise une capture réelle et rejoue les callbacks hors TWS : les quatre passent de 0 % à **100 %**. Les doubles reproduisent les pièges d'IBKR (`NaN` pour un champ absent, `-1` comme sentinelle de non-cotation) au lieu de les lisser — un double qui rendrait `None` ferait passer les gardes du produit pour inutiles. | active |
| D-050 | **Le type de marché est OBSERVÉ, jamais supposé.** `fetch_snapshot` lisait `client.marketDataType`, un attribut qui n'existe pas dans `ib_async` 2.1.0 : le mode retombait silencieusement sur `DELAYED` à chaque appel, et la sonde G5 échouait à chaque exécution réelle. Il se déduit désormais des champs qu'IBKR a remplis (`delayed*`). Limite écrite et conservée : **temps réel et figé remplissent les mêmes champs** — le produit ne revendique jamais `LIVE` sur cette seule base, et l'ignorance est avouée (`TYPE_INCONNU`) plutôt que présentée comme une mesure. | active |
| D-051 | **L'anonymisation d'un artefact G5 n'est pas optionnelle.** Le relevé porte les positions réelles du compte. `enregistrer` anonymise PUIS contrôle, et refuse d'écrire s'il reste une trace — un artefact publié « en espérant » qu'il soit propre finit dans un dépôt public avec un numéro de compte dedans. Du portefeuille, on ne conserve que les **cardinalités** des écarts : compter suffit à prouver que la réconciliation tourne, nommer publierait le portefeuille. | active |
| D-052 | **Deux decisions ne partagent jamais un numero.** Un lot a insere D-028, D-029 et D-030 alors que la base les portait deja : trois identifiants designaient chacun DEUX decisions, et un message de commit citant « D-029 » renvoyait a deux textes differents. Un journal de decisions dont les renvois sont ambigus ne sert plus a expliquer une decision — c'est-a-dire a rien. Les entrees anterieures gardent leur numero, les miennes deviennent D-049 a D-051, et `tests/test_vertex_1_0_decisions.py` refuse desormais un doublon ou un trou. La collision datait de trois lots et n'avait ete vue par personne : aucune relecture humaine ne compte cinquante numeros a la main. | active |
| D-053 | **Une expiration n'est pas une panne, et un instrument doit savoir le dire.** Mesure du 24 août 2026, machine live, produit **inchangé** : `mesurer_surfaces_vides` (délai plat de 8 s) a annoncé successivement **4, 1, 0, 0, 0 puis 5** « surfaces en erreur » — jamais les mêmes. Interrogées une à une avec un délai généreux, toutes répondaient 200 avec leurs données en 2,2 à 5,2 s. L'instrument n'annonçait pas des pannes : il annonçait sa propre patience. C'est le pire défaut possible pour un outil d'audit — celui qui crie quatre pannes imaginaires apprend à son lecteur à ne plus le lire, et la cinquième, réelle, passera avec les autres. `EXPIREE` est désormais séparée d'`ERREUR`, le plafond passe à 60 s (D-024 documente des routes à 9–31 s), et chaque appel porte sa **durée mesurée** — sans quoi aucun avant/après n'était possible. | active |
| D-054 | **Un produit qui n'a pas scanné ne permet aucune conclusion.** Second fantôme mesuré le même jour : `/api/cockpit` et `/api/comite` sortaient « VIDE À EXAMINER » sur un serveur fraîchement redémarré, et se remplissaient dès le scan terminé. Le produit annonçait lui-même son état sur `/healthz` (`last_scan: null`) ; l'instrument ne le lui demandait jamais. La règle porte sur **l'état du produit**, pas sur une liste de surfaces : tenir à la main la liste des surfaces alimentées par le scan finirait par excuser un vide réel. Tant que `last_scan` est nul, la classe « suspecte » est inatteignable. | active |
| D-055 | **Un corpus amputé ne prouve rien, et le silence de l'amputation était le vrai coût.** `mesurer_regles_mortes` rendait `''` pour une page non lue : elle entrait dans le corpus comme une page VIDE, et chaque classe CSS qu'elle seule utilise devenait « jamais apparaissante », donc candidate à la suppression. Un instrument qui propose de supprimer du code à cause de sa propre impatience est pire qu'aucun instrument. Les pages non lues sont désormais nommées, et **toutes les preuves retombent à `INDECIDABLE`** dès qu'il en manque une. Défaut de la correction elle-même, trouvé en l'EXÉCUTANT et non en la relisant : les deux échantillons délibérément inexistants comptaient comme pages manquantes, ce qui aurait amputé le corpus à chaque passage — un outil qui ne conclut jamais ne sert à rien. | active |
| D-056 | **Un seuil qu'on n'a pas mesuré se déclare comme convention.** Le seuil de lenteur vaut 10 s. On a cherché le budget réel du client : il n'y en a aucun — le produit ne contient qu'**un** `AbortController` (`vertex/static/vertex/js/vx-core.js`), au service de l'annulation demandée par l'appelant, et **aucun minuteur ne le déclenche** ; rien n'abandonne donc une requête au bout d'un délai. Écrire « le moment où le navigateur renonce » aurait inventé un fait, ce que D-039 interdit ailleurs pour les dates ; la règle ne change pas parce qu'il s'agit d'une durée. **Corrigé le 25 août 2026** : la première rédaction affirmait « pas un `AbortController` dans toute l'UI », ce qui était faux — le gardien ne balayait que `vertex/ui/**/*.py` et jamais le JavaScript servi sous `/static`, où il vit. Le fond tenait, la preuve était mal bornée. | active |
| D-057 | **« Je n'ai pas mesuré » a son propre code de sortie.** Mesuré le 24 août 2026 : les quatre instruments navigateur sortaient en **code 1** avec 26 lignes de trace Playwright et zéro ligne utile — `mesurer_regles_mortes` après avoir bâti 4,4 Mo de corpus pour rien. Deux mensonges dans un même comportement : une trace brute se lit « le produit a planté », l'inverse exact de la vérité ; et le code 1 se confond avec un vrai plantage alors que leur contrat annonce « 0 = mesuré, 2 = témoin muet ». Un opérateur qui range les codes en « 0 / pas 0 » ne peut alors pas distinguer *mesuré propre* de *jamais mesuré*. Code **3**, la convention déjà posée par `mesurer_g5_live` pour TWS injoignable, avec un aveu de trois lignes qui **nie explicitement** dire quoi que ce soit du produit. Même classe que D-053, autre mécanisme. | active |
| D-058 | **Trois motifs, pas un booléen — parce qu'ils n'appellent pas la même action.** `navigateur_pret()` répondait `False` à « playwright absent », « binaire absent » et « lancement refusé ». Les remèdes sont pourtant `pip install`, `playwright install`, et **rien à installer du tout** : sur cette machine Chromium est présent et refuse simplement de démarrer. Répondre « non » aux trois envoie l'opérateur réinstaller un binaire déjà là. Le contrat booléen de `navigateur_pret()` est conservé — trois bancs s'abstiennent dessus, et changer son type les ferait s'abstenir sur un dict toujours vrai, donc prétendre mesurer ce qu'ils n'ont pas mesuré. | active |
| D-059 | **Un graphe daté vaut mieux qu'une attente de 15 s — à condition de dire qu'il date.** Mesuré le 24 août 2026 sur le produit live : après CHAQUE scan, le premier visiteur de la page Portefeuille attendait **15,1 s** (27,8 s à froid), le second 0,007 s. La clé de fraîcheur inclut `scan_ts`, donc quelqu'un repayait cette attente à chaque cycle. Le graphe du scan précédent est pourtant une réponse utilisable : il est désormais servi **immédiatement** (0,02 s mesuré), marqué `STALE` avec son `age_s` et son `as_of`, pendant qu'une reconstruction se fait hors du chemin de requête. Le tout PREMIER visiteur, lui, attend toujours — il n'y a rien d'honnête à servir, et fabriquer un graphe vide serait l'invention que le produit s'interdit. | active |
| D-060 | **Servir un graphe daté en silence aurait été pire que la lenteur retirée.** La route par symbole assemble sa charge champ par champ : sans y recopier `fraicheur`, `age_s` et `reconstruction_en_cours`, elle aurait présenté du périmé comme courant sur une route entière. Et le pied de la carte passait `Date.now()` — il annonçait « mis à jour maintenant » pour un graphe pouvant dater de plusieurs scans. Il porte désormais l'horodatage réel de construction, un badge « SCAN PRÉCÉDENT » et la mention « recalcul en cours ». Observé à l'écran pendant la fenêtre : « Il y a 6 min · … · SCAN PRÉCÉDENT · recalcul en cours ». | active |
| D-061 | **Le verrou d'un mémo doit couvrir la CONSTRUCTION, pas seulement la lecture.** Il était relâché avant l'appel coûteux : dix visiteurs simultanés fabriquaient dix fois le même graphe, à 15 s pièce. Un banc à dix fils tient désormais le compte à une seule construction. Corollaire écrit avec : une reconstruction qui échoue n'efface jamais le dernier graphe connu — servir daté reste infiniment plus utile qu'une section vide — et un repos de 30 s empêche qu'une panne de source ne devienne un fil par visiteur. | active |
| D-062 | **Quatre cinquièmes des demandes d'options au courtier étaient jetées.** `reqSecDefOptParams` rend les strikes **toutes échéances confondues** ; `terminal.meta` en fait l'union et `chain()` applique cette liste à **chaque** échéance — alors que le pas d'IBKR change avec l'échéance (1 $ sur les hebdomadaires proches, 5 $ ou 10 $ au-delà). Mesuré le 25 août 2026 sur le compte réel : GDDY refuse 97, 98, 99, 101…109 sur ses **trois** échéances — 11 sur 14 demandés, seuls 100, 105 et 110 existent ; MRNA, ORCL, ECHO et NRG suivent le même motif, « tout sauf les multiples de 5 ». **214 refus sur 250 lignes de journal.** Cette file est la même que celle des requêtes interactives : d'où les fiches mesurées à 30–46 s pour un contenu que yfinance rend en 1,3 s. `vertex/options/strike_memory.py` retient ce que le courtier a refusé, par (titre, échéance). | active |
| D-063 | **Une mémoire de refus ne doit ni inventer, ni aveugler.** Elle ne fait que **filtrer** une liste proposée ailleurs : déduire « le pas est de 5, donc 155 existe » fabriquerait un contrat que personne n'a listé. Trois gardes tiennent l'autre bord — un refus **expire** (une échéance gagne des strikes quand le sous-jacent s'éloigne) ; un strike qui qualifie **efface** immédiatement le refus antérieur ; et si tout ce qui est proposé est connu comme refusé, on **redemande quand même**. Sans cette dernière, une mémoire prise pendant une coupure du courtier — où tout échoue — viderait la chaîne en silence pendant six heures, ce qui serait pire que le gaspillage corrigé. | active |
| D-064 | **Un correctif invisible doit se mesurer quelque part.** Le lot D-062 se voit dans ce qui **n'arrive plus** : le seul moyen de l'observer était de compter les « Aucune définition de titre » sur deux journaux du courtier, à la main — donc personne ne l'aurait fait, et le lot serait resté une intention. `strike_memory.statistiques()` est exposé sur `/api/system/diagnostics` et rendu sur la page Système. Mesure du 25 août 2026, sur le produit live après redémarrage : **701 strikes demandés, 268 refus mémorisés sur 30 couples (39 % des demandes déjà faites ont été refusées), 20 évités** — 2,9 % à cet instant. Ce chiffre est le **début de la courbe**, pas son régime : la plupart des couples en sont à leur premier passage, qu'ils paient plein tarif pour alimenter la mémoire. | active |
| D-065 | **« Je n'ai rien mesuré » ne s'affiche pas « 0 % ».** `part_evitee_pct` vaut `None` tant qu'aucun strike n'a été proposé, et l'écran a une branche dédiée (« aucune mesure encore »). Rendre 0 % ferait passer une ignorance pour un résultat — même faute que D-054 sur `scan_fait`. Deuxième garde du même esprit : une redemande faute de mieux n'est **jamais** comptée comme une économie, puisqu'on refait alors le travail entier ; elle a son propre compteur, parce qu'un chiffre qui grimperait sans cesse dirait que la mémoire ne sert plus à rien. | active |
| D-066 | **Une requête de page ne collecte pas.** `AUDIT-TOTAL-2026-08-25` P0.1, mesuré sur le SHA `73de92f5`, compte réel, TWS ouvert : `/api/ticker/<sym>` coûtait 3,3–3,6 s au calme mais **28–48 s sous charge**, 6,1 s **à chaque appel** sous coupure réseau, et cinq demandes **simultanées du même titre** faisaient cinq collectes — dont une à **136,9 s**. `vertex/app/snapshot.py` sert un instantané daté, coalesce par clé, sert le rassis pendant la reconstruction et avoue l'absence. Après : p95 chaud **0,024 s** (cible 0,400) et première réponse froide **0,003 s** (cible 1,500). | active |
| D-067 | **Le coût était payé par quelqu'un qui ne s'en servait pas.** Mesuré : `options_pack` seul vaut 46,6 s sur un titre neuf, contre 3,4 s pour tout le reste du dossier — et la page Analyse lit `detail`, `company`, `peers_data`, `sector_median`, `risk_map`, `in_universe`, mais **`pack` zéro fois**. La clé reste servie, parce que retirer une clé casse un contrat qu'on ne peut pas auditer entièrement ; elle vient désormais du magasin, jamais du chemin synchrone. | active |
| D-068 | **Périmer par la dépendance, pas seulement par l'horloge.** Le graphe de connaissance avait sa PROPRE implantation stale-while-revalidate ; la fiche en réclamait une seconde, et deux implantations du même mécanisme divergent toujours. Le graphe est migré sur le magasin partagé. Il a fallu pour cela un `jeton` de validité : le graphe est déterministe pour un scan donné, et c'est le scan qui le périme. Sans lui, changer de clé à chaque scan aurait transformé un graphe parfaitement utilisable en entrée « absente », donc en attente de 15 s. | active |
| D-069 | **Accélérer une route réveille les courses qu'elle endormait.** La fiche mettait 3 à 43 s à revenir : `VXCharts` avait toujours fini de charger avant. À 3 ms, la course se perd — et `/api/client-log` a montré `VXCharts is not defined`. La garde n'protégeait que le côté GAUCHE d'un `||` : quand l'objet manque, l'opérateur évalue la droite et lève. Même forme que D-031. Le repli AVOUE au lieu de sortir de `loadDossier()` : un `return` là aurait emporté thèse, catalyseurs et plan avec le graphique. | active |
| D-070 | **Une borne de relance se règle sur la mesure, pas sur l'intuition.** Le client garde la charge en cache jusqu'à 30 min : sans relance, la première visite d'un titre serait restée un écran creux toute la session. Première borne posée à 6 × 5 s = 30 s — elle **perdait la course** contre une collecte mesurée entre 17 et 46 s, et la fiche affichait « en cours » alors que l'API était complète. Portée à dix tentatives à recul progressif, ~130 s. Au-delà, l'écran dit que la collecte n'a pas abouti et pointe vers les sources. | active |
| D-071 | **Les 60 dépôts proposés sont des références, pas une base à fusionner.** Chaque projet reçoit ADOPTER/PILOTER/INSPIRER/RECHERCHE/REJETER dans `roadmap/USER-REPOSITORIES-2026-08-25.md`. Toute surface d'ordre, 0DTE, vente nue ou contrôle broker par LLM est exclue, même si le dépôt contient une idée utile. | active |
| D-072 | **Une route interactive ne collecte pas le monde.** Les appels lourds IBKR/Yahoo/options sortent du chemin synchrone : la page sert un snapshot daté, puis déclenche un refresh borné, coalescé et observable. `api/ticker` est le premier cas P0. | active |
| D-073 | **Les ETF sont first-class.** Leur décision exige holdings point-in-time, look-through, overlap, frais, tracking, liquidité, structure, domicile et devise ; une simple fiche action renommée ETF n'est pas conforme. | active |
| D-074 | **Un dépôt public sans `LICENSE` ne donne aucun droit implicite de copie.** Vertex choisit sa propre licence et vérifie licence/attribution au SHA de toute dépendance avant redistribution ou contribution externe. | active |
| D-075 | **La collision de numéros s'est reproduite ENTRE BRANCHES, et le gardien ne pouvait pas la voir.** `tests/test_vertex_1_0_decisions.py` vérifie l'unicité **dans un fichier** ; il était vert des deux côtés. La pile #795–#809 et le blueprint ont pourtant écrit tous deux des décisions à partir de D-031, et le conflit n'est apparu qu'à la fusion. Règle appliquée, la même qu'en D-052 : les entrées **antérieures** gardent leur numéro — les miennes datent du 24 août 13:39, celles du blueprint du 25 août 10:47, qui deviennent donc D-071 à D-074. Ce qu'un gardien de fichier ne peut pas attraper, seule la fusion le révèle : c'est une raison de fusionner tôt, pas une raison de se croire couvert. | active |
| D-076 | **Ce que la source ignore reste ignoré.** `AUDIT-TOTAL-2026-08-25` P0.2. Mesuré le 25 août 2026 : les fondamentaux ne portaient qu'un `provenance.as_of` **au niveau du lot**, égal à `utc_now_iso()` — l'instant de RÉCEPTION du paquet entier ; deux titres collectés à six heures d'écart partageaient la même date. Chaque titre porte désormais `source` et `recu_a` mesurés, et `observe_a`/`available_at` restent **`None`** parce que `yfinance.info` ne les donne pas. Les remplir avec l'instant de réception — l'erreur naturelle, et celle que le `as_of` de lot commettait de fait — ferait croire qu'un P/E lu aujourd'hui était connaissable aujourd'hui, alors qu'il reflète un dépôt dont la date de publication n'est pas dans la charge. | active |
| D-077 | **Une garde posée avant la brèche, comme D-040.** Recensement du 25 août 2026 : **aucun** des cinq modules d'évaluation historique (`track_record`, `walk_forward_validation`, `evidence_lab`, `out_of_sample`, `probability_calibration`) ne lit les fondamentaux. Le pire cas décrit par l'audit n'est donc pas actif, et le dire évite de prétendre fermer une brèche ouverte. `exiger_disponibilite()` refuse une valeur dont la date de disponibilité est inconnue — un **refus**, pas un avertissement, car « aucun nouveau score historique ne doit contourner ce socle » est une intention tant que rien ne l'empêche. Le refus dit aussi que la valeur reste utilisable au PRÉSENT : sans cette phrase, un lecteur conclurait qu'elle est fausse. | active |
| D-078 | **Un dossier vide n'est pas un dossier absent.** `ZZZZ_INEXISTANT` — 404 chez Yahoo — ressortait avec les **quatorze champs à `null` et aucun marqueur d'erreur**, strictement indiscernable d'un titre réel dont les fondamentaux manquent. `QUALITY_STANDARD` §1 exige « erreur ou raison de l'absence » ; `erreur` la porte. **Hypothèse réfutée en la mesurant** : je pensais qu'un symbole en échec était silencieusement retiré du lot ; c'est faux, `yfinance.info` rend un dict même sur 404. Le défaut réel était l'inverse — un dossier bien présent, entièrement vide, et muet. Corollaire : les médianes sectorielles comptent désormais `n_pe` et `n_ecartes`, parce qu'une médiane sur trois titres d'un secteur qui en compte quarante n'est pas la médiane du secteur. | active |
| D-079 | **Ne pas demander au courtier ce qu'il ne peut pas connaître — et le défaut était de moi.** Le scan télécharge l'historique des indices, matières premières et macro par le MÊME chemin que les actions : `fetch_universe_bars` recevait donc `^TNX`, `^VIX`, `GC=F`, `BTC-USD`, `DX-Y.NYB` — des conventions yfinance — et `_contrat`, écrit au lot 1, tentait **trois** qualifications d'action US pour chacun, à chaque scan, dans la file partagée avec les requêtes interactives. Vérifié AVANT d'écrire le filtre : sur les 517 symboles de l'univers, **zéro** ne commence par `^`, ne finit par `=F` ou `-USD`, ne contient de point ; les deux seuls à porter un tiret sont `BRK-B` et `BF-B`, que `_forme_ibkr` traite déjà. Le filtre n'écarte donc aucun titre réel, et un banc le tient sur l'univers entier — si celui-ci gagne un jour un symbole de cette forme, il le dira avant que le filtre ne le mange en silence. Les écartés sont **nommés** dans `non_actions`, jamais confondus avec les `inconnus` : on ne les a pas demandés, IBKR ne les a pas refusés. | active |
| D-080 | **Un champ toujours absent rend son critère inapplicable, en silence.** `fetch_contract_details` rendait `open_interest=None` **en dur** — case « NON COUVERT » du protocole G5, dont le motif décrivait le symptôme et non la cause : IBKR sert très bien l'open interest, mais **uniquement** si l'appelant demande le tick générique **101**. `reqTickers` ne le demande pas ; le board de production l'obtient depuis toujours par `reqMktData(genericTickList='100,101,106')`. `QUALITY_STANDARD` §3 exige l'OI pour une option candidate et le mandat en fait un critère de liquidité : un critère dont l'entrée est toujours absente ne filtre rien. Corrigé en demandant les mêmes ticks que le chemin éprouvé, avec sa marge mesurée (2,6 s) et une annulation en `finally` — une ligne de marché laissée ouverte est une ressource bornée et partagée. | active |
| D-081 | **Zéro contrat ouvert n'est pas une absence de donnée.** Trois distinctions tenues par des témoins : `NaN` et la sentinelle `-1` du courtier deviennent `None` ; un open interest **réellement nul** est conservé, parce que « aucun contrat ouvert » est une information décisive quand on juge la liquidité d'une option ; et un call ne reçoit jamais l'intérêt ouvert des puts — IBKR expose les deux côtés séparément, et lire le mauvais donnerait un chiffre plausible et faux, la pire espèce. Les cotations des témoins sont **fabriquées et le disent** : la capture réelle n'en portait pas, et on ne complète pas un artefact de preuve avec des chiffres inventés. | active |
| D-082 | **398 octets qui en rendaient 800 000.** `parse_rss` lit du XML **distant et non fiable** — Vertex va chercher lui-même le flux Google News — et passait par `minidom.parseString`, dont l'expansion d'entités est active. Mesuré le 25 août 2026 sur le vrai code : 233 o → 800 o (×100) à trois niveaux, **398 o → 800 000 o (×100 000)** à six ; chaque niveau supplémentaire multiplie par dix, et neuf niveaux tiennent dans 563 octets pour 800 Mo. C'est un *billion laughs* atteignable depuis un flux que le produit va chercher. Le parseur refuse désormais **toute déclaration de type de document** : un flux RSS n'en a jamais besoin, et la refuser à la racine supprime expansion d'entités, entités externes et références récursives d'un seul geste. Plafond de taille en amont : un flux hostile ne doit pas être lu du tout, pas seulement mal lu. | active |
| D-083 | **Un durcissement qui ne durcit rien est pire que pas de durcissement : il rassure.** Première correction : poser les gestionnaires sur `ET.XMLParser().parser`. En Python 3.12, `XMLParser` est l'implantation C et **n'expose pas** `.parser` — le `getattr(..., None)` rendait le durcissement **silencieusement inopérant**, et la mesure d'après montrait l'expansion intacte. C'est la MESURE qui l'a dit, pas la relecture. D'où un banc qui vérifie l'**effet** (une charge hostile ne passe pas) et non la présence d'une API. Réécrit sur `expat` directement. Et pas de `defusedxml` : une dépendance nouvelle exige licence, pin, audit et rollback, alors que le refus posé ici est plus **strict** que son défaut — qui interdit les entités mais parse encore le DTD. | active |
| D-084 | **L'écran de sécurité affirmait une protection absente.** La carte « Verrou d'accès » disait, sans code : « par sécurité, le serveur n'écoute que **127.0.0.1** (pas d'accès WiFi/LAN) ». C'est **faux** dès que `VERTEX_LAN=1` ou `PORT` est posé — deux cas où il n'y a **ni code ni restriction**. Le message de démarrage, lui, annonçait « VERTEX_LAN=1 — SANS code ! » pour une ouverture causée par `PORT`, en nommant une variable non définie : l'opérateur la cherche dans son `.env`, ne la trouve pas, conclut que le message est périmé, et son desk reste ouvert. `config_validation` servait la même promesse figée en troisième exemplaire. Trois copies, aucune ne lisait l'état réel. | active |
| D-085 | **Un texte qui n'est pas dérivé de l'état qu'il décrit finit toujours par mentir.** La cause n'était pas la règle d'écoute — elle est juste et reste **inchangée**, `PORT` compris, parce qu'un hébergeur comme Render impose ce port et exige `0.0.0.0`. La cause était qu'elle était *décrite* à trois endroits qui ne la lisaient pas. `vertex/app/exposition.py` en devient le propriétaire unique : hôte, ouverture, motif, protection et le cas dangereux `expose_sans_code` — nommé, pour qu'un écran puisse le dire au lieu de promettre le contraire. Deux bancs interdisent désormais à `terminal.py` et à la carte de recalculer la règle eux-mêmes. | active |
| D-086 | **Un seul outil d'échappement, dans le noyau servi.** Recensement du 25 août 2026 : **cinq** interpolations `${…}` d'un champ d'origine externe sans `esc()` dans `vertex/ui` et `vertex/static`. Chaque page portait sa **propre** copie d'`esc`, et le JavaScript servi sous `/static` n'en avait **aucune** — les notifications du shell interpolaient `n.title`, `n.message` et `n.category` bruts. `VX.esc` vit désormais dans `vx-core.js`. Une fonction d'échappement recopiée diverge : il suffit qu'une copie oublie l'apostrophe. Deux fichiers `/static` touchés, donc bump obligatoire du service worker (v214 → v215) — le gardien d'empreinte l'a imposé, et c'est exactement son office. | active |
| D-087 | **Gravité dite honnêtement : c'est la classe qu'on ferme, pas un exploit vivant.** Le seul champ réellement externe des cinq est le `sector` de `yfinance.info` — et Yahoo n'est pas hostile ; les autres viennent des tickers que l'utilisateur saisit lui-même. Au pire une auto-injection, plus probablement un panneau de notifications cassé en silence par une apostrophe. **Aucun chemin d'attaquant distant n'est prouvé**, et le dire évite de faire passer une hygiène pour une urgence. Le ticker n'entre dans l'`onclick` que s'il **ressemble à un ticker** : échapper en HTML ne suffirait pas, le parseur décode `&#39;` avant que JS ne voie la chaîne. | active |
| D-088 | **Un gardien qui compte les faux positifs devient du bruit, puis est ignoré.** Le recensement signalait `${state.sector===s?'selected':''}` — une expression qui **mentionne** un champ externe mais ne rend que `'selected'` ou `''`. L'échapper aurait été échapper un littéral. Le recensement retire donc la **condition** d'un ternaire avant de chercher, et un témoin garde la nuance dans les deux sens : une condition est ignorée, une **branche** qui rend le champ reste attrapée. | active |
| D-089 | **Une page tierce pouvait écrire dans le bureau, et c'est démontré.** Vertex tourne en local pendant que l'utilisateur navigue ; toute page visitée peut faire émettre un POST vers `localhost:5002` — requête *simple*, donc **sans preflight CORS** — dès lors qu'elle utilise `text/plain`. Or `/api/desk` lit son corps avec `get_json(force=True)`, et `force` **ignore le `Content-Type`**. Mesuré le 25 août 2026 sur le vrai produit : `POST /api/desk` avec `Origin: https://site-malveillant.example` → **200, écriture effective**. Quatorze routes `POST` existent ; aucune ne vérifiait l'origine. | active |
| D-090 | **Protéger un cookie qui n'existe pas ne protège rien.** `SESSION_COOKIE_SAMESITE='Lax'` était en place — mais sans `VERTEX_CODE`, la configuration par défaut, **il n'y a pas de session**, donc rien à protéger. Un jeton CSRF exigerait cet état absent. Le navigateur, lui, **envoie toujours `Origin` sur un POST cross-origin** : le comparer à l'hôte servi est la protection exacte pour une application locale, sans dépendance ni état. Une écriture **sans** `Origin` reste acceptée — `curl`, les bancs, les appels serveur-à-serveur — parce qu'un navigateur ne peut pas l'omettre : son absence est la marque d'un client qui n'est pas une page web, pas un contournement. Le port fait partie de l'identité ; le schéma non, sans quoi un proxy TLS casserait l'accès. | active |
| D-091 | **Une protection d'écriture qui n'en couvre que certaines ne protège rien.** Posée dans la fabrique, avant les blueprints : c'est la route qu'on oublie qui sert de porte, et route par route la quinzième s'écrira sans garde. Ce qui limitait déjà les dégâts est dit aussi : le lot 362 empêche un push partiel d'effacer les clés absentes, et un instantané `desk_avantperte_*.json` précède l'écriture — les données n'étaient pas **détruites**. Ce qui restait possible était d'**injecter** un faux trade ou une fausse entrée dans le registre qui sert à juger les décisions passées. | active |
| D-092 | **Le numéro de compte IBKR réel de l'utilisateur était en clair dans un dépôt PUBLIC.** Mesuré le 25 août 2026 : `git grep` le trouve dans **sept fichiers suivis**, et `gh api repos/… --jq .private` rend **`false`**. C'est une violation directe de l'invariant `CLAUDE.md` — « aucun secret ou donnée de compte dans Git » — et de `QUALITY_STANDARD` §9. **La plupart de ces fichiers sont de moi** : je les ai écrits en documentant mes mesures sur le compte réel, tout en faisant respecter cet invariant ailleurs. Le cas le plus net : un banc qui vérifie que l'anonymiseur **retire** un numéro de compte, et qui, pour le vérifier, en écrivait un vrai. | active |
| D-093 | **Un numéro FABRIQUÉ prouve exactement la même chose.** Les bancs emploient désormais `U8000001` : une chaîne qui ressemble à un identifiant de compte, ce qui est tout ce que le masquage a besoin de rencontrer. Les documents et docstrings reçoivent le masque existant `U<masque>` — aucune valeur n'y était utile. Les 90 bancs concernés passent sans changement d'intention. **Gravité dite honnêtement** : un numéro de compte IBKR seul n'ouvre aucun accès — il faut des identifiants et un second facteur. Ce n'est pas une clé ; c'est une donnée de compte identifiante, publiée, que la doctrine interdit. Cela suffit à la retirer, et ne suffit pas à crier à la compromission. | active |
| D-094 | **Le gardien doit se soumettre à sa propre règle — ma première version ne s'y soumettait pas.** Le recensement `test_vertex_1_0_compte_hors_git` tient le motif à zéro sur l'arbre suivi. Sa contre-épreuve « le motif reconnaît un vrai numéro » écrivait… le vrai numéro, et **passait au vert** uniquement parce que le fichier n'était pas encore suivi par Git : au premier `git add`, mon propre gardien réintroduisait ce qu'il interdit. La forme de contrôle est désormais construite par concaténation, et un banc vérifie que **ce fichier-là** ne contient aucun numéro réel. Un gardien exempté de sa règle est le prochain coupable. **Limite déclarée, et elle est réelle** : le recensement ne voit que les fichiers **suivis** ; l'historique Git, lui, **contient toujours le numéro**. Le retirer exige une réécriture d'historique — décision humaine, pas correction — et le dépôt étant public depuis des semaines, il a pu être cloné ou indexé. Nettoyer les fichiers empêche la **prochaine** publication ; cela n'annule pas les précédentes. **Décision humaine restante : `HUMAN_REQUIRED`** — réécrire ou non l'historique. | active |
| D-095 | **0,35 % était lu comme 35 %, et ça changeait la taille de position.** Mesuré le 26 août 2026 sur la vraie source (`yfinance 1.5.2`) : `dividendYield` est un **pourcentage** (KO 2.3, MO 6.19, AAPL 0.35), tandis que `trailingAnnualDividendYield` est une **fraction** (0.022611, 0.061925, 0.003383). **Les deux unités coexistent dans la même charge** — c'est ce qui rend le piège durable : un relecteur peut vérifier « yfinance rend une fraction » et avoir raison, sur l'autre champ. `fundamentals.py` publiait le champ ambigu brut et `analysis.py` le testait contre `0.02`, seuil écrit pour une fraction : « ≥ 2 % » devenait « ≥ 0,02 % ». AAPL et GOOGL touchaient le bonus défensif **plein**, comme MO à 6,19 %, et la branche de repli `else (1 if _div else 0)` exigeait un rendement sous 0,02 % — **branche morte**. Balayage de 3 780 configurations réalistes : **900 bascules de verdict** (24 %), dix secteurs ; pour AAPL et GOOGL, **13 conditions de marché sur 25**. Ce n'est pas cosmétique : `profile` alimente `decision_stack`, qui s'en sert pour ouvrir une décision d'option et pour `_size_hint` — la **taille de position affichée** (STRONG_BUY : 5-8 % en OFFENSIF contre 4-6 % en DÉFENSIF). | active |
| D-096 | **Pas un `/100` : un rendement dérivé de la charge, qui nomme sa source.** Diviser par cent serait se rendre otage d'une version de bibliothèque — le jour où `yfinance` repasse à la fraction, la correction devient le défaut, en silence et dans l'autre sens. `vertex/data_sources/rendement_dividende.py` devient le propriétaire unique de l'unité et procède par ordre de **certitude** : champ non ambigu, puis reconstruction `dividendRate/prix`, puis le champ ambigu — étiqueté `unite_inferee`, jamais présenté comme mesuré. Sur les quatre titres réels, aucune inférence n'est nécessaire. Une valeur hors de tout rendement plausible est **refusée** plutôt que convertie au hasard : un chiffre plausible et faux est la pire espèce. **Zéro n'est pas l'inconnu** — un titre qui ne verse rien rend `0.0`, un rendement ignoré rend `None`, même distinction qu'en D-081 pour l'open interest. Deux producteurs lisaient le même champ ambigu (`data_sources/fundamentals.py`, `data/company.py`) ; un banc leur interdit désormais de le relire, sans quoi le troisième héritera du piège. | active |
| D-097 | **Le simulateur affirmait intégrer un dividende qu'il n'appliquait jamais — D-084 rejoué sur le pricing.** `scenario_pricer.LIMITATIONS` déclarait dans **chaque** réponse servie « dividendes intégrés via un rendement continu ». Or aucun appelant de production ne renseigne `UnderlyingSetup.dividend_yield` : `options_intel_api` et `redesign` construisent le setup sans ce champ, qui vaut donc `0.0` ; seuls des bancs le passaient. La phrase est désormais **dérivée du `q` employé** (`limitations_pour`), et un banc interdit qu'elle retourne dans la constante — la cause n'était pas le texte, c'est qu'il ne dérivait pas de l'état qu'il décrit (D-085). Le sens de l'erreur est dit, pas seulement son existence : sans dividende, la prime d'un call est **surestimée**. Mesuré à DTE 180, la cible du mandat, ATM, IV 30 % : ignorer 3 % de rendement surévalue le call de **8,9 %**, 7 % de **19,8 %**, et l'effet **croît avec l'échéance** — donc il est maximal précisément sur l'horizon visé (DTE préféré 120–240). **Limite assumée : ce lot rend la phrase vraie, il ne câble pas `q` dans le pricing.** Passer le rendement au simulateur changerait les primes affichées jusqu'à 20 % ; c'est un changement de calcul, il mérite son propre lot avec ses preuves avant/après, et non d'être glissé dans une correction d'unité. | active |
| D-098 | **La courbe des taux était collectée, affichée, et ignorée.** `data_sources/rates.py` expose une couche de taux par échéance honnête — elle marque `fallback_used=True` quand elle sert son taux plat documenté de 4,5 %. Mais elle n'a jamais rien d'autre à servir : recensement du 26 août 2026, `RateCurve(` n'est construite **qu'une seule fois dans tout le dépôt**, sans points (`scenario_pricer.py:109`). Toute la couche était **inerte**. Or le scan remplit déjà `scan_state['macro']` avec `^IRX`, `^FVX`, `^TNX`, `^TYX`, et la page Marchés en dessine l'échelle des maturités : la donnée était sous la main, et le simulateur tournait à côté sur une constante. Mesure du jour : 3M 3,705 %, 5A 4,351 %, 10A 4,639 %, 30A 5,174 % ; taux réel à 180 jours **3,738 %**, soit **76 points de base** au-dessus dans le repli. `vertex/data_sources/courbe_taux.py` bâtit la courbe sur ce qui est **déjà collecté** — aucun appel réseau, une requête de page ne collecte pas (D-072). Moins de deux points exploitables ⇒ retour au repli plat documenté : le chemin dégradé reste exactement celui d'avant. | active |
| D-099 | **Le simulateur disait qu'au stop, on était gagnant.** Les deux sites de production ne passaient à `scenario_pricer` ni courbe ni rendement. Mesuré sur la **vraie route** `/api/options/simulate` (KO call 95, DTE 180, IV 30 %, mid 4,20) : prime courante 7,120 → **6,465** (−9,20 %) ; prime au stop 4,545 → 4,074 (−10,36 %) ; gain de base attendu 123,7 % → 105,6 %. Et surtout : `worst_planned_loss_pct` valait **+2,7 %** — la perte maximale planifiée, **au stop choisi par l'utilisateur**, ressortait **positive**. Le simulateur affirmait qu'en touchant son propre stop on était encore gagnant. Parce que cette « perte » était un gain, `reward_risk` était **incalculable** (`None`). Entrées mesurées : **−7,7 %** et un ratio de 13,71. Les deux erreurs poussent dans le même sens pour un call, et Vertex est un produit d'achat de calls longs à risque borné : la surévaluation portait systématiquement sur ce que l'utilisateur achète, et croît avec l'échéance — donc maximale sur l'horizon visé (DTE 120–240). | active |
| D-100 | **Un prix corrigé sans provenance serait aussi opaque qu'un prix faux.** `vertex/options/entrees_mesurees.py` devient le propriétaire unique des entrées de pricing : deux routes qui liraient l'état chacune de leur côté finiraient par servir deux prix différents pour le même contrat. Chaque simulation porte désormais un bloc `entrees` disant d'où vient le taux, à quelle date, et si le dividende a été **appliqué** — avec le motif quand il ne l'a pas été. Un rendement **inconnu** reste `None` et n'est pas appliqué : le confondre avec `0.0` ferait passer « je ne sais pas » pour « ce titre ne verse rien » (D-081). La couverture **avoue** le trou réel de la courbe : aucun point entre 3 mois et 5 ans, et l'échéance visée par le produit tombe en plein dedans — interpoler sur cet intervalle est bien meilleur qu'une constante à 4,5 %, et reste une approximation ; le dire est la condition pour s'en servir. **Ce lot change des chiffres affichés** : il exige une acceptation humaine explicite, et son rollback est un `revert` propre. | active |
| D-101 | **Les deux autres moteurs de pricing tournaient aussi sur les constantes.** Recensement du 26 août 2026, après D-099 : `engines/multileg_lab` et `options/double_prob` portent les mêmes `R_DEFAULT=0.045` / `Q_DEFAULT=0.0`, et **trois routes vivantes** les appelaient sans jamais leur passer autre chose — `/api/options/strategies/<sym>` et `/api/options/analyze` (payoff, PoP et Greeks des **positions réelles du desk**), `/api/options/scanners` (**probabilité de doublement** des cinq meilleurs candidats). Mesuré : la probabilité de doublement était surévaluée de **8,3 %** sur KO (q=2,26 %), de **20,9 %** sur MO (q=6,19 %), de 12,1 % sur un KO à 365 jours ; côté multi-jambes, probabilité de profit 35,2 % → 32,6 %, delta −6,5 %, theta +15,4 %, et le **vomma changeait de signe** (−0,58 → +0,63). L'écart va toujours dans le sens optimiste pour un call long — la structure que Vertex recommande. | active |
| D-102 | **Ce n'était pas un mensonge, et le dire évite de compter deux fois la même faute.** Contrairement à `scenario_pricer` (D-097), ces deux moteurs **tracent déjà** `r` et `q` dans leur bloc `model` : ils disent la vérité sur ce qu'ils ont employé. Le défaut est en amont — l'appelant ne leur a jamais donné que la constante. `entrees_mesurees.taux(scan_state, echeance)` leur sert le point interpolé de **leur propre échéance** (35 j : 3,710 % ; 180 j : 3,743 % ; 365 j : 3,811 %), ce que la couche §6.6 promettait depuis le début. `strategies_for_symbol` transmet désormais `r` et `q` à son délégué, sans quoi la correction s'arrêterait à la porte du moteur ; un banc l'exige. | active |
| D-103 | **Le chemin dégradé est identique au bit près, et c'est vérifié.** Le repli plat de la courbe vaut `0.045` — exactement `R_DEFAULT` des deux moteurs. Un desk sans scan retrouve donc le chiffre qu'il avait, et un banc l'affirme sur cinq échéances. `/api/options/analyze` ne connaît pas le titre : la charge accepte désormais un `sym` **optionnel** pour le dividende, et sans lui `q` reste `0.0` — valeur que le bloc `model` trace, donc déclarée et non dissimulée. Le taux, lui, ne dépend pas du titre et s'applique dans tous les cas. Les charges du client actuel, qui n'envoie pas `sym`, continuent de passer : un banc de compatibilité le tient. | active |
| D-104 | **Le plus modélisé des quatre moteurs de prix ne déclarait rien.** Recensement du 26 août 2026 : `scenario_pricer` trace `model_source`, `rate`, `limitations` et son bloc `entrees` ; `multileg_lab` et `double_prob` tracent un bloc `model` avec `r`, `q` et `calibrated`. `strategy/legacy_adapter` — servi par la page Stratégie et par `/api/command` — n'en déclarait **aucun**. Or chez lui **rien ne vient d'une cotation** : le strike est CHOISI par balayage pour viser un delta cible, la prime est CALCULÉE en Black-Scholes, et l'IV est DÉDUITE de l'ATR (`_iv_proxy(atr) = atr/100 × √252`, bornée à [0,22 ; 1,10]). Détail qui rendait la confusion inévitable : la sortie servait `'iv': round(sig*100)`, **le même nom de champ** que l'`iv` des contrats du board — qui est, elle, une cotation. Même clé, deux sens, aucune distinction. | active |
| D-105 | **L'ATR mesure le réalisé ; une prime se paie sur l'implicite.** Mesuré sur les **578 contrats réellement cotés** du board du 25 août 2026 : l'IV médiane par titre s'étale de **2,52×** entre p10 (0,248) et p90 (0,625), et sur cet intervalle la prime d'un call ATM à 180 jours passe de 8,016 à 18,312 — **+128 %**. Le proxy assigne pourtant l'IV depuis le **seul ATR** : deux titres de même ATR reçoivent la même IV, quelle que soit celle que le marché cote ; sous 1 % d'ATR le plancher 0,22 cesse même de les distinguer. Cette sensibilité (+128 %) **écrase** celle au taux (76 pb, D-098) : c'est l'IV proxy, et non le taux, la principale source d'incertitude de ce moteur — et c'est ce que les limitations doivent dire, avec le **sens** de chaque écart, parce que « le modèle a des limites » n'informe personne. | active |
| D-106 | **Un chiffre modélisé qui prend la forme d'un montant engagé doit dire qu'il est modélisé.** Cette sortie ne sert pas qu'un prix : elle porte un **dimensionnement en dollars** — nombre de contrats, coût, `maxloss` — sur des budgets de 5 k et 15 k, puis de 50 k à 200 k dans `build_portfolio`. Le taux déclaré est **lu** dans `legacy_engine.R`, jamais recopié : une déclaration qui porterait sa propre copie finirait par annoncer autre chose que ce qui a été calculé (D-084). **Aucun chiffre ne change dans ce lot** — un banc l'exige en recalculant la prime servie. Recâbler taux et dividende ici déplacerait des primes déjà entièrement synthétiques, et la sensibilité à l'IV l'emporte de toute façon : la déclaration d'abord, le recâblage sera son propre lot s'il a lieu. Un recensement interdit désormais à **tout** moteur de servir un prix sans dire son modèle — le cinquième s'y heurtera. | active |
